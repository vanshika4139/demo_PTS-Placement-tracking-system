document.addEventListener('DOMContentLoaded', () => {
  const form = document.querySelector('.auth-form');
  if (!form) return;
  // Real login ab Flask backend handle karta hai (form ka default POST submit hone do)
  // JS se koi interception nahi chahiye
});